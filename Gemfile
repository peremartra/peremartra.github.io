source "https://rubygems.org"

# Use GitHub Pages gem which includes Jekyll and all compatible dependencies
gem "github-pages", group: :jekyll_plugins

# Optional: Add timezone data for Windows users
gem "tzinfo-data", platforms: [:mingw, :mswin, :x64_mingw, :jruby]
